# frontend/pages/.htaccess
# Ensure PHP files in this directory are executed by the server.
<FilesMatch "\.php$">
    SetHandler application/x-httpd-php
</FilesMatch>

# Prevent direct access to source files.
<FilesMatch "\.(json|log|env|md)$">
    Order allow,deny
    Deny from all
</FilesMatch>
